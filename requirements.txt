# Dependencias Python. Instalar con: pip install -r requirements.txt
#
# (1) MARCO DE EVALUACIÓN + utilidades (puro Python, sin GPU)
numpy>=1.24,<2.0
pyyaml>=6.0
matplotlib>=3.7
scipy>=1.10          # smooth_trajectory.py (filtro Savitzky-Golay)
evo>=1.28            # referencia de evaluación (el GT colineal usa el método along-track propio)

# (2) PIPELINE DPVO métrico — la red corre sobre el fork DPVO (ver SETUP en
#     README). torch/torchvision se instalan con la rueda CUDA correcta para
#     tu GPU (https://pytorch.org); el fork DPVO trae sus extensiones CUDA.
torch>=2.3            # instalar la rueda +cuXXX acorde a tu CUDA
torchvision
opencv-python>=4.8    # svo_to_stereo_pngs.py
einops                # dependencia del fork DPVO
yacs                  # dependencia del fork DPVO (configs)

# (3) ZED SDK — pyzed NO se instala por pip: viene con el instalador del
#     ZED SDK de Stereolabs (https://www.stereolabs.com/developers).
#     Sólo necesario para run_zed_pt.py (baseline ZED PT) y
#     svo_to_stereo_pngs.py (conversión SVO -> PNGs estéreo).
# pyzed   (instalar vía ZED SDK, no pip)
